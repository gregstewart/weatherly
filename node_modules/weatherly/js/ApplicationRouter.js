'use strict';
var viewBuilder = require('weatherly/js/model/ViewBuilder');

var ApplicationRouter = Backbone.Router.extend({
    routes: {
        '': 'index',
    },

    index: function () {
        var view = viewBuilder.build.todaysWeatherForecastView();
        view.render();
    }
});

module.exports = ApplicationRouter;